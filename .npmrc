# npm configuration
# Note: The "production" config warning is harmless and comes from Railway's build environment
# DevDependencies (TypeScript) are needed for the build, so we don't set production=true

